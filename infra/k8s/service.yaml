apiVersion: v1
kind: Service
metadata:
  name: ranepa-app
  namespace: ranepa-${APP_SLUG}
  labels:
    app: ranepa-app
    slot: ${APP_SLUG}
spec:
  type: ClusterIP
  selector:
    app: ranepa-app
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
