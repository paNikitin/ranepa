# Активный план: ranepa
#
# Что уже собрано и работает в проде — см. archived.txt.
# Здесь — только то, что ещё нужно сделать перед мероприятием.

================================================================================
СОСТОЯНИЕ НА 2026-05-10
================================================================================

Инфра целиком жива:
  - https://ranepa.gigaparsers.ru/         → лендинг (200, 5 карточек)
  - https://ranepa.gigaparsers.ru/app1..5/ → шаблон-эпп каждого слота (200)
  - https://ranepa.gigaparsers.ru/dev/app1..5/ → ttyd с Claude
    (basic-auth, --dangerously-skip-permissions, VLESS-туннель)

End-to-end проверено: с каждого code-server pod'а scripts/deploy.sh
успешно собирает (DinD), пушит в Harbor (--insecure-registry для
self-signed), делает kubectl apply в свой ns (RBAC изолирует слоты).

================================================================================
ОТКРЫТЫЕ ВОПРОСЫ К ЮЗЕРУ
================================================================================

[ ] u9.  Брендбук РАНХиГС — если есть PDF/гайд, прислать. Иначе возьму
         публичные цвета с ranepa.ru и дефолтный шрифт.
         Применяется правкой одного слоя — app/src/styles/brand.css +
         public/brand/{favicon.svg,apple-touch-icon.png}; никакого
         кода-перетряхивания.

[ ] u13. Имена/email/Telegram 5 участников РАНХиГС — нужны для:
         а) подписей на лендинге (сейчас «Участник 1..5» placeholder);
         б) понять кому какой APP_SLUG/пароль раздать.

[ ] u14. Дата мероприятия. Сейчас всё работает; перед событием полезно
         будет провести dry-run с одним «фейковым» участником
         (см. остатки фазы 8) и понять остаётся ли запас по Max 5x
         лимитам OAuth.

================================================================================
ОСТАЛОСЬ СДЕЛАТЬ
================================================================================

[ ] L1. Лендинг — подменить placeholder-имена на реальные участников
        (после получения списка от u13).
        Файл: infra/landing/index.html (5 карточек).
        Деплой: docker build → push landing:0.1.1 → kubectl set image
                deploy/ranepa-landing landing=...:0.1.1 -n ranepa.

[ ] L2. Брендинг шаблона — РАНХиГС цвета/лого/шрифт
        (после u9). Файлы:
          app/src/styles/brand.css — переменные --brand-primary и др.
          app/public/brand/favicon.svg, apple-touch-icon.png
          app/index.html — title, apple-mobile-web-app-title
          app/public/manifest.webmanifest — name, theme_color
        После правки — git push, code-server'ы при рестарте pull'нут;
        каждый участник передеплоит из своей сессии.

[ ] L3. nginx persist на сервере — запушить локальный коммит cd9182ea8
        в gigaparser-репозитории (gitlab.parsers360.ru/gigaparsers/
        gigaparser) после ревью. Без этого следующий rebuild edge-nginx
        потеряет наш ranepa-блок и WS-headers (хотя в текущем
        runtime'е всё работает через docker cp + nginx -s reload).
        Тоже — там вмержен naisty-look блок, который тоже был drift'ом.

[ ] L4. Harbor robot token — записать в
        gitlab_setup/k3s_prod/initial_deploy_sequence.sh рядом с другими
        push-роботами (подобно look-bot). Сейчас токен живёт только
        в k8s secret harbor-creds-ranepa в наших ns'ах.

================================================================================
ФАЗА 8 — оставшиеся пункты тестового прогона
================================================================================

[ ] 8.2. Lighthouse mobile с iPhone Safari: PWA ≥ 90, Performance ≥ 90.
        Запустить хотя бы для одного слота после брендинга.

[ ] 8.3. «Поделиться → На экран Домой» с iPhone — иконка должна быть
        правильная (apple-touch-icon), запуск в standalone (без
        адресной строки).

[ ] 8.4. Прогнать сценарий «AI всё сломал» → попросить агента в чате
        вернуть всё к шаблону → агент запускает scripts/reset.sh
        → перерождение.

[ ] 8.5. Прогнать промпты из examples/prompts.md по списку — убедиться
        что каждый разворачивается во что-то осмысленное (без правок
        промптов).

================================================================================
ОПЦИОНАЛЬНО (НЕ блокирует мероприятие)
================================================================================

[ ] OPT1. Per-slot custom URL'ы (если u13 даст имена участников —
          можно вместо app1..5 называть слоты по фамилиям, например
          /ivanov, /petrov; потребует перегенерации серта только если
          переехать на поддомены).

[ ] OPT2. Лимит токенов на слот (защита от случайной бесконечной
          итерации claude). Можно через настройку
          ANTHROPIC_RATE_LIMIT или через monitoring + автоматический
          kill-switch если usage > N tokens/час.

[ ] OPT3. Тёплый кэш npm install / docker layers — сейчас при первом
          deploy.sh каждый pod скачивает ~150Mb npm + базовые слои.
          Можно подложить .npmrc с локальным registry или nexus-mirror.

[ ] OPT4. Backup текущего состояния каждого слота в git перед
          deploy.sh — чтобы при рестарте pod'а (когда git reset --hard
          стирает несохранённое) можно было вернуться.
          Сейчас deploy.sh + commit'ы участника нет — изменения живут
          только в Harbor-образе, не в git. Это намеренно, но при
          желании можно добавить.
